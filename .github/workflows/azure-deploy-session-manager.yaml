name: Deploy (session-manager)

on:
  workflow_dispatch:

jobs:
  deploy_to_azure:
    name: Deploy to Azure
    uses: pagopa/io-std/.github/workflows/deploy-workspace.yaml@main
    with:
      environment: io-p-session-manager
      workspace-name: session-manager
    secrets: inherit