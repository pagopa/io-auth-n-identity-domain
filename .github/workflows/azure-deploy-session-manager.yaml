name: Deploy (session-manager)

on:
  workflow_dispatch:

jobs:
  deploy_to_azure:
    name: Deploy to Azure
    uses: ./azure-deploy-workspace-template.yaml
    with:
      environment: session-manager-prod-cd
      workspace-name: '@pagopa/io-session-manager'
    secrets: inherit