name: PR - Opex Dashboard Fims

on:
  pull_request:
    types:
      - opened
      - edited
      - synchronize
      - reopened
    branches:
      - main
    paths:
      - "apps/session-manager/api/fims.yaml"
      - ".github/workflows/pr_opex_api_fims.yaml"
      - ".github/workflows/opex_api_fims.yaml"
      - ".opex/fims/**"

  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: false

jobs:
  plan:
    uses: ./.github/workflows/call_pr_opex_api.yml
    name: Plan changes against Production
    secrets: inherit
    with:
      api_name: fims
