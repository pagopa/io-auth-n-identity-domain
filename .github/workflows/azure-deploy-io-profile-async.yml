name: Deploy (io-profile-async)

on:
  workflow_dispatch:

jobs:
  deploy_to_azure:
    name: Deploy to Azure Function App
    uses: pagopa/dx/.github/workflows/function_app_deploy.yaml@2503864ad284f0c19315f4e87b9e9e2addb45f47
    with:
      # '-cd' suffix added inside the pipeline
      environment: apps-prod
      resource_group_name: "io-p-itn-auth-main-rg-01"
      workspace_name: "io-profile-async"
      function_app_name: "io-p-itn-auth-profas-func-02"
      health_check_path: "/info"
      use_staging_slot: true
      use_private_agent: true
    secrets: inherit
