meta {
  name: getUserDataProcessing
  type: http
  seq: 10
}

get {
  url: {{IO_PROFILE_BASE_URL}}/user-data-processing/{{FISCAL_CODE}}/DOWNLOAD
  body: none
  auth: inherit
}

body:json {
  {
    "choice": "DOWNLOAD"
  }
}

script:pre-request {
  const ioProfileApiKey = bru.getEnvVar("IO_PROFILE_API_KEY")
  if (ioProfileApiKey != null ) {
    req.setHeader("x-functions-key", ioProfileApiKey)
  }
  
}
