meta {
  name: deleteUserDataProcessing
  type: http
  seq: 11
}

delete {
  url: {{IO_PROFILE_BASE_URL}}/user-data-processing/{{FISCAL_CODE}}/DELETE
  body: none
  auth: inherit
}

body:json {
  {
    "choice": "DOWNLOAD"
  }
}

script:pre-request {
  const ioProfileApiKey = bru.getEnvVar("IO_PROFILE_API_KEY")
  if (ioProfileApiKey != null ) {
    req.setHeader("x-functions-key", ioProfileApiKey)
  }
  
}
