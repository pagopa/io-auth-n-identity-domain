meta {
  name: GetSession
  type: http
  seq: 4
}

get {
  url: {{IO_SESSION_MANAGER_OI_BASE_URL}}/api/auth/v2/session
  body: none
  auth: inherit
}

params:query {
  ~fields: 
}

script:pre-request {
  req.setMaxRedirects(0);
}

settings {
  encodeUrl: true
  timeout: 0
}
